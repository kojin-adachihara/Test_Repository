version: 2
jobs:
  build:
    docker:
      - image: php:5.6.23
    steps:
      - checkout
  test:
    docker:
      - image: php:5.6.23
    steps:
      - checkout
      - run: vendor/bin/phpunit Arithmetic_test
  deploy:
    machine: True
    steps:
      - run: echo deploy
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy:
          requires:
            - test
          filters:
            branches:
              only: master
