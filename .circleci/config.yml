version: 2
jobs:
  build:
    docker:
      - image: php:5.6.23
    steps:
      - checkout
      - run:
          command: echo deploy
  test:
    docker:
      - image: php:5.6.23
    steps:
      - checkout
      - run:
          command: echo deploy
  deploy:
    machine: True
    steps:
      - run: dir
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy:
          requires:
            - test
          filters:
            branches:
              only: master
